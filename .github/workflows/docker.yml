name: Build and Deploy Blog App

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

jobs:
  # ==========================================
  # STAGE 1: Build JAR & Docker Image (CI)
  # ==========================================
  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Build JAR with Maven
        run: mvn clean package -DskipTests

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ vars.DOCKERHUB_USERNAME }}/blog-app:latest
            ${{ vars.DOCKERHUB_USERNAME }}/blog-app:${{ github.sha }}

#  # ==========================================
#  # STAGE 2: Deploy to AWS EC2 (CD)
#  # ==========================================
#  deploy:
#    name: Deploy to EC2
#    needs: build-and-push
#    runs-on: ubuntu-latest
#
#    steps:
#      - name: SSH and Deploy to EC2
#        uses: appleboy/ssh-action@v1.0.3
#        with:
#          host: ${{ secrets.EC2_HOST }}
#          username: ${{ secrets.EC2_USER }}
#          key: ${{ secrets.EC2_SSH_KEY }}
#          script: |
#            cd /home/${{ secrets.EC2_USER }}/blog-app
#            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/blog-app:latest
#            docker compose up -d --no-deps --build blog-app
#            docker image prune -f
